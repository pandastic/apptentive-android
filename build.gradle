buildscript {
    repositories {
        mavenCentral()
    }   

    dependencies {
        classpath 'com.android.tools.build:gradle:0.7.0'
    }   
}

apply plugin: 'android-library'

dependencies {
    compile 'com.android.support:support-v4:13.0.0'
}

android {
    compileSdkVersion 19
    buildToolsVersion "19.0.0"

    defaultConfig {
        minSdkVersion 7
        targetSdkVersion 16
    }

    sourceSets {
        main {
            manifest.srcFile 'apptentive-android-sdk/AndroidManifest.xml'
            java.srcDirs = ['apptentive-android-sdk/src']
            resources.srcDirs = ['apptentive-android-sdk/src']
            res.srcDirs = ['apptentive-android-sdk/res']
        }
    }
}